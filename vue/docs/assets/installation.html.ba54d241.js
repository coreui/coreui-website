import{_ as e,e as o}from"./app.48efcc98.js";const r={},t=o('<h2 id="tooling-setup" tabindex="-1">Tooling setup <a class="anchor-link" href="#tooling-setup" aria-hidden="true">#</a></h2><p>CoreUI Vue Admin Templates uses <a href="https://docs.npmjs.com/misc/scripts/" target="_blank" rel="noopener noreferrer">npm scripts</a> for its build system. Our <code>package.json</code> includes convenient methods for working with the framework, including compiling code, running tests, and more.</p><p>To use our build system and run our admin template locally, you&#39;ll need a copy of source files and Node. Follow these steps and you should be ready to rock:</p><ol><li><a href="https://nodejs.org/en/download/" target="_blank" rel="noopener noreferrer">Download and install Node.js</a>, which we use to manage our dependencies.</li><li>If you prefer to use Yarn instead of NPM, you have to <a href="https://classic.yarnpkg.com/lang/en/docs/install/" target="_blank" rel="noopener noreferrer">download and install it</a></li><li><a href="https://coreui.io/product/free-vue-admin-template/" target="_blank" rel="noopener noreferrer">Download CoreUI Vue Admin Template</a> or <a href="https://coreui.io/download/" target="_blank" rel="noopener noreferrer">Download CoreUI PRO Vue Admin Template</a>.</li><li>Navigate to the root template directory and run <code>npm install</code> or <code>yarn install</code> to install our local dependencies listed in <code>package.json</code>.</li></ol><p>When completed, you&#39;ll be able to run the various commands provided from the command line.</p><h2 id="using-npm-scripts" tabindex="-1">Using npm scripts <a class="anchor-link" href="#using-npm-scripts" aria-hidden="true">#</a></h2><p>Our <code>package.json</code> includes numerous tasks for developing the project. Run <code>npm run</code> or <code>yarn run</code> to see all the npm scripts in your terminal. <strong>Primary tasks include:</strong></p><table class="table table-striped table-api"><thead><tr><th>Task</th><th>Description</th></tr></thead><tbody><tr><td><code>npm run serve</code> or <code>yarn serve</code></td><td>Compiles CSS and JavaScript, builds the documentation, and starts a local server.</td></tr><tr><td><code>npm run build</code> or <code>yarn build</code></td><td>Creates the <code>dist/</code> directory with compiled files. Uses <a href="https://sass-lang.com/" target="_blank" rel="noopener noreferrer">Sass</a>, <a href="https://github.com/postcss/autoprefixer" target="_blank" rel="noopener noreferrer">Autoprefixer</a>.</td></tr><tr><td><code>npm test</code> or <code>yarn test</code></td><td>Runs tests locally</td></tr></tbody></table><h2 id="sass" tabindex="-1">Sass <a class="anchor-link" href="#sass" aria-hidden="true">#</a></h2><p>CoreUI uses <a href="https://sass-lang.com/dart-sass" target="_blank" rel="noopener noreferrer">Dart Sass</a> for compiling our Sass source files into CSS files (included in our build process), and we recommend you do the same if you&#39;re compiling Sass using your own asset pipeline.</p><p>Dart Sass uses a rounding precision of 10 and for efficiency reasons does not allow adjustment of this value. We don&#39;t lower this precision during further processing of our generated CSS, such as during minification, but if you chose to do so we recommend maintaining a precision of at least 6 to prevent issues with browser rounding.</p><h2 id="autoprefixer" tabindex="-1">Autoprefixer <a class="anchor-link" href="#autoprefixer" aria-hidden="true">#</a></h2><p>We uses [Autoprefixer][autoprefixer] (included in our build process) to automatically add vendor prefixes to some CSS properties at build time. Doing so saves us time and code by allowing us to write key parts of our CSS a single time while eliminating the need for vendor mixins like those found in v3.</p><p>We maintain the list of browsers supported through Autoprefixer in a separate file within our GitHub repository. See <code>.browserslistrc</code>.</p><h2 id="local-template" tabindex="-1">Local template <a class="anchor-link" href="#local-template" aria-hidden="true">#</a></h2><p>Here&#39;s how to get it started:</p><ol><li>Run through the <a href="#tooling-setup">tooling setup</a> above to install all dependencies.</li><li>From the root template directory, run <code>npm run start</code> or <code>yarn start</code> in the command line.</li><li>Open <code>http://localhost:8080</code> in your browser, and voil\xE0.</li></ol><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="anchor-link" href="#troubleshooting" aria-hidden="true">#</a></h2><p>Should you encounter problems with installing dependencies, uninstall all previous dependency versions (global and local). Then, rerun <code>npm install</code> or <code>yarn install</code>.</p>',19);function n(a,s){return t}var l=e(r,[["render",n],["__file","installation.html.vue"]]);export{l as default};
